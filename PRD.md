# 우리 동네 카페 지도 서비스 — PRD (Product Requirements Document)

- 문서 버전: v0.1 (초안)
- 작성일: 2026-07-27
- 상태: 초안 — 코드 작성 전, "오픈 이슈" 섹션 확인/답변 필요

---

## 목차

1. 개요
2. 용어 정의
3. 사용자 및 권한 구분
4. 사용자 시나리오
5. 기능 요구사항
6. 화면 구성
7. 데이터 모델
8. 인증(Auth) 설계
9. Row Level Security(RLS) 정책
10. 외부 연동 (카카오맵 / 지오코딩)
11. 환경변수 및 키 관리
12. 엑셀 재업로드 & 중복 처리 정책
13. 비기능 요구사항
14. 제약사항 / Out of Scope
15. 오픈 이슈 (구현 전 확인 필요)

---

## 1. 개요

### 1.1 배경 및 목적
동네 카페 목록을 엑셀로 관리해오던 방식에서 벗어나, 엑셀에 정리된 카페 정보(이름/주소/카테고리)를 업로드하면 자동으로 지도 좌표로 변환되어 카카오맵 위에 마커로 표시되는 웹 서비스를 만든다. 사용자는 지도 위에서 카페를 방문했는지 체크하고, 한줄 소감을 남기고 조회할 수 있다.

### 1.2 서비스 한 줄 정의
"엑셀로 등록된 동네 카페를 지도에서 보고, 방문 여부와 소감을 기록/공유하는 서비스"

### 1.3 기술 스택 (제약사항으로 확정된 항목)
| 영역 | 선택 |
|---|---|
| 프론트엔드 프레임워크 | React + Vite |
| UI | Tailwind CSS + shadcn/ui |
| 엑셀 파싱 | xlsx |
| 지도/좌표 변환 | 카카오맵 JavaScript SDK (프론트엔드에서 직접 호출, `libraries=services` 포함 로드) |
| 데이터 저장 | Supabase Database (PostgreSQL) + RLS |
| 인증 | Supabase Auth (이메일/비밀번호) |
| 배포 | Vercel |
| 키/시크릿 관리 | `.env` (로컬, `.gitignore` 포함) + Vercel 환경변수 (배포) |
| Supabase 스키마/RLS 관리 | Supabase MCP 도구로 수행 |

---

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| 카페 목록(Cafe) | 엑셀 업로드로 등록되는 이름/주소/카테고리 단위 레코드. 서비스 전체에서 공유되는 **공용 데이터** |
| 지오코딩(Geocoding) | 주소 문자열을 위도/경도 좌표로 변환하는 과정 (카카오 로컬 API 사용) |
| 방문 기록(Visit) | 특정 로그인 사용자가 특정 카페에 대해 남기는 "방문 여부 체크 + 한줄 소감" 레코드. **사용자별로 소유되는 데이터** |
| 관리자(Admin) | 엑셀 업로드 권한을 가진 사용자 |
| 일반 사용자(Member) | 회원가입 후 방문 체크/소감만 남길 수 있는 사용자 |
| 비로그인 방문자(Guest) | 로그인하지 않은 채 지도와 공개된 방문 기록을 열람만 하는 사용자 |

---

## 3. 사용자 및 권한 구분

사용자 확인 질문에 대한 답변을 반영하여 다음과 같이 확정한다.

- **카페 목록은 공용(전체 공개) 데이터이며, 업로드는 관리자만 가능하다.** 일반 회원은 엑셀을 업로드할 수 없다.
- **회원가입/로그인은 이메일 + 비밀번호 방식**을 사용한다 (Supabase Auth의 Email/Password Provider).
- **방문 체크 여부와 한줄 소감은 전체 공개**된다. 로그인하지 않은 사용자도 지도에서 "누가 방문했는지, 어떤 소감을 남겼는지" 열람할 수 있다. 단, 작성/수정/삭제는 본인만 가능하다.

| 권한 | 카페 목록 열람 | 방문/소감 열람 | 방문/소감 작성·수정 | 엑셀 업로드 |
|---|---|---|---|---|
| Guest (비로그인) | 가능 | 가능 | 불가 | 불가 |
| Member (일반 회원) | 가능 | 가능 | **본인 레코드만** | 불가 |
| Admin (관리자) | 가능 | 가능 | 본인 레코드만 | **가능** |

> **확정**: 관리자 판별은 환경변수 이메일 화이트리스트 방식(`VITE_ADMIN_EMAILS`)으로 결정했다. 프론트엔드는 이 값으로 업로드 버튼 노출 여부만 결정하고, 실제 강제는 `cafes` 테이블 RLS 정책에 동일한 이메일을 하드코딩하는 것으로 한다(§7.3의 `profiles` 테이블은 도입하지 않음). 현재 관리자: `admin@naver.com`.

---

## 4. 사용자 시나리오

### 4.1 관리자 — 카페 목록 등록
1. 관리자가 로그인 후 "카페 목록 관리" 화면에서 엑셀 파일(이름/주소/카테고리 컬럼)을 업로드한다.
2. 시스템이 각 행의 주소를 카카오 지오코딩 API로 좌표 변환한다.
3. 변환에 성공한 카페는 지도에 마커로 표시되고, 실패한 주소는 화면에 별도 목록으로 안내된다.
4. 관리자는 실패 목록을 확인하고 주소를 수정하여 재업로드할 수 있다.

### 4.2 일반 사용자 — 방문 체크 및 소감 작성
1. 비로그인 상태에서도 지도에서 마커를 보고 클릭하면 카페 정보와 기존 방문자들의 소감을 볼 수 있다.
2. 소감을 작성하려면 로그인이 필요하다 → 로그인/회원가입 유도.
3. 로그인 후 같은 마커를 클릭하면 "방문 여부 체크" 토글과 "한줄 소감" 입력창이 나타난다.
4. 입력 후 저장하면 Supabase에 본인 사용자 ID로 레코드가 저장(또는 갱신)된다.
5. 이후 같은 카페 마커를 다시 클릭하면, 이전에 본인이 입력했던 체크 상태와 소감이 그대로 불러와져 보인다(수정 가능).

### 4.3 일반 사용자 — 내가 방문한 카페 모아보기
1. 로그인한 사용자가 "내 방문 목록" 메뉴로 이동한다.
2. 엑셀 업로드 없이도, 본인이 방문 체크한 카페만 리스트 형태로 조회된다 (지도 없이 목록 UI, 또는 목록+지도 병행 — [오픈 이슈] 참고).
3. 목록에서 카페를 선택하면 지도상 해당 위치로 이동하거나 상세 소감을 볼 수 있다.

---

## 5. 기능 요구사항

### F1. 엑셀 업로드 & 카페 목록 파싱 (관리자 전용)
- `.xlsx` 파일 업로드 지원 (파일 파싱은 프론트엔드에서 처리, 예: SheetJS 계열 라이브러리)
- 필수 컬럼: **이름(name), 주소(address), 카테고리(category)**
- 업로드 전 컬럼 검증: 필수 컬럼 누락 시 업로드 중단 및 에러 메시지 표시
- 파싱된 데이터는 미리보기 후 "등록" 확정 단계를 거친다 (실수 방지)

### F2. 지오코딩 & 지도 마커 표시
- 카카오맵 JS SDK의 `services.Geocoder`(주소 검색) 사용, **프론트엔드에서 직접 호출**
- 등록된 모든 카페 좌표를 지도 로드시 마커로 표시
- 지오코딩 실패(좌표 변환 불가) 주소는:
  - 별도 "변환 실패 목록" 섹션에 카페명+주소와 함께 노출
  - 지도에는 표시되지 않음
- 카테고리별 마커 아이콘/색상 구분은 MVP 범위에 포함할지 [오픈 이슈]에서 확인

### F3. 마커 클릭 → 방문 체크 + 한줄 소감
- 마커 클릭 시 인포윈도우/패널에 다음 표시:
  - 카페 이름, 주소, 카테고리
  - 방문 여부 체크박스(토글)
  - 한줄 소감 입력창 (텍스트, 글자 수 제한 필요 — [오픈 이슈])
  - 다른 사용자들의 방문 체크 인원수 및 소감 목록 (전체 공개이므로)
- 비로그인 상태에서는 체크박스/입력창이 비활성화되고 로그인 유도 문구 표시

### F4. 회원가입/로그인 및 소감 영속성
- 이메일/비밀번호 기반 회원가입, 로그인, 로그아웃
- 로그인한 사용자가 같은 카페(카페 ID 기준)에 재방문(마커 재클릭) 시, 본인이 이전에 저장한 체크 상태·소감이 입력창에 그대로 로드됨
- 저장 시 **동일 사용자 + 동일 카페 조합은 항상 1개의 레코드로 upsert** (신규 생성이 아니라 갱신)

### F5. 내 방문 목록 화면
- 로그인 사용자 전용 화면
- 엑셀 파일 유무와 무관하게, 현재 DB에 등록된 카페 중 **본인이 "방문함"으로 체크한 카페만** 필터링하여 목록으로 표시
- 목록 항목: 카페 이름, 주소, 카테고리, 본인 소감, (선택) 지도에서 보기 링크

---

## 6. 화면 구성

| 화면 | 설명 | 접근 권한 |
|---|---|---|
| 지도 메인 | 전체 카페 마커 표시, 지오코딩 실패 안내 배너 | 전체 공개 |
| 로그인 / 회원가입 | 이메일/비밀번호 폼 | 전체 공개 |
| 마커 상세 팝업 | 카페 정보 + 방문 체크/소감 입력 + 타인 소감 목록 | 열람: 전체 / 작성: 로그인 |
| 카페 목록 관리 (엑셀 업로드) | 엑셀 업로드, 파싱 미리보기, 지오코딩 실패 목록 | 관리자 전용 |
| 내 방문 목록 | 본인이 체크한 카페 목록 | 로그인 사용자 전용 |

---

## 7. 데이터 모델 (Supabase / PostgreSQL)

### 7.1 `cafes` (공용 카페 목록)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK, default gen_random_uuid()) | |
| name | text, not null | 카페 이름 |
| address | text, not null | 원본 주소 (엑셀 값) |
| category | text | 카테고리 |
| lat | double precision, nullable | 지오코딩 성공 시 위도 |
| lng | double precision, nullable | 지오코딩 성공 시 경도 |
| geocode_status | text ('success' \| 'failed') | 좌표 변환 결과 |
| created_at | timestamptz, default now() | |
| updated_at | timestamptz, default now() | |

- **유니크 제약**: `(name, address)` — 동일 이름+주소는 한 행만 존재 ([중복 처리 정책](#12-엑셀-재업로드--중복-처리-정책) 참고)

### 7.2 `visits` (사용자별 방문 체크 + 소감)
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| user_id | uuid, not null, FK → auth.users(id) | 작성자 |
| cafe_id | uuid, not null, FK → cafes(id) | 대상 카페 |
| visited | boolean, default true | 방문 여부 체크 |
| comment | text | 한줄 소감 |
| created_at | timestamptz, default now() | |
| updated_at | timestamptz, default now() | |

- **유니크 제약**: `(user_id, cafe_id)` — "같은 사람이 같은 장소(카페)에 남긴 기록은 1개만 유지"를 DB 레벨에서 강제
  - 카페 매칭은 `cafe_id`(FK)로 하되, `cafes` 테이블 자체가 `(name, address)` 유니크이므로 결과적으로 "이름+주소" 기준 1인 1레코드 요구사항을 만족

### 7.3 (선택) `profiles` — 관리자 판별용
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK), FK → auth.users(id) | |
| role | text ('admin' \| 'member') | 관리자 여부 판별 |

> profiles 테이블 도입 여부는 [오픈 이슈] 참고 — 대안으로 환경변수 기반 관리자 이메일 화이트리스트도 가능.

---

## 8. 인증(Auth) 설계

- Supabase Auth의 **이메일/비밀번호** 프로바이더 사용
- 회원가입 시 이메일 인증(컨펌 메일) 사용 여부는 [오픈 이슈]에서 확인
- 프론트엔드는 Supabase JS 클라이언트로 세션을 관리하고, `visits` 테이블 접근 시 로그인 세션의 JWT를 통해 RLS가 자동 적용됨

---

## 9. Row Level Security(RLS) 정책

### 9.1 `cafes` 테이블
| 작업 | 정책 |
|---|---|
| SELECT | 모두 허용 (anon 포함) — 공용 목록이므로 |
| INSERT / UPDATE / DELETE | 관리자만 허용 (`auth.uid()`가 관리자인 경우만) |

### 9.2 `visits` 테이블
| 작업 | 정책 |
|---|---|
| SELECT | 모두 허용 (anon 포함) — 방문 체크/소감이 전체 공개이므로 |
| INSERT | `auth.uid() = user_id` 인 경우만 허용 (로그인 필수) |
| UPDATE | `auth.uid() = user_id` 인 본인 행만 허용 |
| DELETE | `auth.uid() = user_id` 인 본인 행만 허용 |

- `(user_id, cafe_id)` 유니크 제약과 결합하여, 저장 시 **upsert(ON CONFLICT)** 로 처리 → "재클릭 시 이전 소감이 보이고, 수정 시 레코드가 늘지 않고 갱신"이 보장됨

---

## 10. 외부 연동 (카카오맵 / 지오코딩)

- **지도 표시**: 카카오맵 JavaScript SDK (`//dapi.kakao.com/v2/maps/sdk.js?appkey=...&libraries=services`) 를 프론트엔드에서 로드. **반드시 `libraries=services`를 포함**해야 지오코더(`kakao.maps.services.Geocoder`)를 사용할 수 있음
- **지오코딩**: 카카오맵 SDK의 `services` 라이브러리(`kakao.maps.services.Geocoder`) 사용 — 별도 백엔드 프록시 없이 프론트엔드에서 직접 호출. 여러 주소를 한 번에 변환할 때도 **동시 병렬 호출이 아닌 하나씩 순차 처리**하며, 실패한 주소는 즉시 실패 목록에 반영
- 카카오 개발자 콘솔에서 발급받은 **JavaScript 키**를 사용 (REST API 키가 아님에 유의 — 지도 SDK는 JS 키, 서버사이드 REST 지오코딩을 쓸 경우 REST 키가 별도 필요하므로 방식 확정 시 재확인)

---

## 11. 환경변수 및 키 관리

| 키 | 용도 | 저장 위치 |
|---|---|---|
| `VITE_KAKAO_MAP_KEY` | 카카오맵 JS SDK appkey | `.env` (로컬) / Vercel 환경변수 (배포) |
| `VITE_SUPABASE_URL` | Supabase 프로젝트 URL | 동일 |
| `VITE_SUPABASE_ANON_KEY` | Supabase anon key (RLS로 보호되므로 공개 가능한 키) | 동일 |

- Vite 프로젝트이므로 클라이언트에 노출되는 환경변수는 `VITE_` 접두사를 사용 (Next.js의 `NEXT_PUBLIC_`이 아님)
- 카카오 개발자 콘솔에서 **플랫폼(도메인) 제한 설정**으로 키 오남용 방지 필요
- `.env`, `.env.local` 등은 `.gitignore`에 포함하여 저장소에 커밋되지 않도록 함
- Vercel 배포 시 동일한 키를 프로젝트 환경변수(Production/Preview/Development)에 등록

---

## 12. 엑셀 재업로드 & 중복 처리 정책

확인된 정책: **이름 + 주소 기준 병합(upsert)**

- 재업로드된 엑셀의 각 행에 대해 `(name, address)`가 기존 `cafes` 테이블에 이미 존재하면:
  - 기존 행을 **갱신**(카테고리 등 변경 사항 반영), `id`는 유지
  - 기존에 쌓인 `visits`(방문 체크/소감)는 `cafe_id`가 그대로 유지되므로 **보존됨**
- `(name, address)` 조합이 새로운 경우: 신규 카페로 **추가**, 지오코딩 수행
- 이전 업로드에는 있었지만 새 엑셀에는 없는 카페의 처리 방식(그대로 유지 / 삭제 / 비활성 표시)은 [오픈 이슈]에서 확인 필요

---

## 13. 비기능 요구사항

- **성능**: 카카오 지오코딩 API는 요청당 호출 제한(쿼터)이 있으므로, 엑셀 대량 업로드 시 순차/배치 처리 및 실패 재시도 로직 고려
- **에러 처리**: 지오코딩 실패, 엑셀 형식 오류, 네트워크 오류 각각에 대한 사용자 친화적 메시지 필요
- **보안**: RLS로 `visits` 쓰기 권한을 본인으로 제한, 카카오 키는 도메인 제한 설정으로 보호
- **반응형 UI**: 모바일 브라우저 대응 확정(§15 오픈 이슈 7번 참고) — 헤더, 지도/사이드바 레이아웃, 로그인·방문소감 팝업이 좁은 화면에서 세로로 쌓이고 여백을 확보하도록 구현됨

---

## 14. 제약사항 / Out of Scope (MVP 기준)

- 카페 정보 수정/삭제 UI (관리자가 개별 카페를 수정하는 화면)는 MVP 범위에서 제외 — 재업로드로만 갱신
- 소감에 대한 좋아요/댓글/신고 등 커뮤니티 기능 없음
- 카테고리 필터, 검색, 정렬 등 지도 UX 고도화 기능은 MVP 이후 과제
- 이 문서는 코드/구현을 포함하지 않으며, 요구사항 정의만을 목적으로 함

---

## 15. 오픈 이슈 (구현 전 확인 필요)

아래 항목은 PRD 작성 중 합리적으로 가정했으나, 코드 작성 전 반드시 확인이 필요합니다.

1. ~~관리자 판별 방법~~ — **확정**: 환경변수 이메일 화이트리스트(§3 참고).
2. **엑셀 컬럼 형식**: 헤더명이 정확히 "이름/주소/카테고리"(한글) 또는 "name/address/category"(영문) 중 어느 쪽을 기준으로 파싱해야 하나요? 컬럼 순서 고정 여부도 확인 필요합니다.
3. **소감 글자 수 제한**: "한줄 소감"의 최대 글자 수 제한이 있나요? (예: 50자)
4. **회원가입 이메일 인증**: 가입 시 이메일 인증(컨펌 메일) 절차를 포함할지, 즉시 가입 완료로 처리할지 확인 필요합니다.
5. **재업로드 시 삭제된 카페 처리**: 새 엑셀에 더 이상 없는 기존 카페를 그대로 둘지, 비활성 처리할지, 삭제할지 확인 필요합니다.
6. **카테고리 활용 범위**: 지도에서 카테고리별 마커 색상/아이콘 구분이나 필터 기능이 MVP에 포함되나요, 단순 정보 표시로 충분한가요?
7. ~~모바일 반응형 지원 여부~~ — **확정**: 모바일 브라우저 대응 포함. 헤더는 좁은 화면에서 줄바꿈/말줄임 처리, 지도와 "내 방문 목록"은 데스크톱(`sm` 이상)에서만 가로 배치하고 모바일에서는 세로로 쌓임, 로그인/방문소감 팝업은 화면 양 끝에 붙지 않도록 좌우 여백과 세로 스크롤(최대 높이) 처리.
8. **"내 방문 목록" 화면에 지도 포함 여부**: 순수 리스트(표)만 필요한지, 목록 클릭 시 지도로 이동하는 기능까지 필요한지 확인 필요합니다.
9. **엑셀 파일 크기/행 수 제한**: 대량 업로드(예: 수백~수천 행) 대비 지오코딩 호출량 관리 정책이 필요한지 확인 필요합니다.
